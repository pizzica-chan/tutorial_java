# このプロジェクトのルール

`.cursor/rules/` にある Cursor 用ルールを、Claude Code でも同じ基準で適用するためのファイル。
各見出しに「適用対象」を明記した。対象外のファイルを触るときは無視してよい。

---

## 造語をしない（常時適用）

その場で作った複合名詞を、見出しや用語にしない。辞書・慣用・現場で通じる語、この教材の用語集（`terms.ts`）にある語は使ってよい。

判断に迷うのは、接尾のパターンである。`確認先` `全区間` `連絡先` のように、普通に使われる複合は造語ではない。一方、動詞の句を縮めて見慣れない名詞にするのは造語である。

```
# 悪い（その場で作った名詞）
調べ始め
読み始めを決める

# よい（慣用の複合、または普通の文）
確認先
全区間
最初に確認すること
どこを先に確認するか
```

- だめなのは「調べる」＋「始め」→「調べ始め」のように、句を圧縮した新しい名詞
- `〜先` `〜区間` を一律禁止しない。確認先・提出先・全区間のように慣用なら使う
- 定着した語は使ってよい: 読み方、切り分け、突き合わせ、スタックトレース、コンテキストパス、処理の入口
- 迷ったら短い普通の文にする。慣用の語を、説明的な言い換えに直さない

## 曖昧な文言修正は案を出す（常時適用）

「いまいち」「直して」「断定的すぎる」「意味が分からない」のように、何が悪いかは言っているが**置き換える文言が無い**ときは、自分で1案を選んで書き換えてはいけない。先に修正案を複数出す。

```
# 悪い
タイトルがいまいち → 自分で「どこまで届いたか」に変えてコミット相当の編集をする

# よい
タイトルがいまいち → 2〜4案を出し、推奨を1つ示して選んでもらう
```

- 案は 2〜4。各案に、なぜその言い方かを一文添える。推奨は先頭か明示する
- 選ばれるまで本文は変えない
- 置き換えが具体なら、案は出さずそのとおり直す（例:「疑わしい箇所として」「『どこまで届いたか』に変えて」）
- 誤字・明らかな文法・すでに決まっている表記ルールの適用は、曖昧ではない。そのまま直す

## 想定読者と認知負荷（常時適用）

想定読者は Web アプリの入門者である。ゼロからアプリを作る人ではなく、HTTP・Java Web アプリ・既存コードの読み方や切り分けを学ぶ人を想定する。

文章は、読者の認知負荷が低くなるよう書く。ただし、簡略化のために事実を曲げない。一般論と実装の差は「一般論と実装の書き分け」のとおり書き分ける。

### 認知負荷を下げる

- 1段落で扱うことを絞る。新概念は一度に1つずつ足す
- 専門語は初出で短く示すか、用語ヒント（`terms.ts`）に載せる語にする
- 手順の前に「いま分かっていること」を置く。シナリオ章の型に合わせる
- 長い修飾句より、短い文を並べる
- 見出しと箇条書きで塊を分ける。目次だけ読んでも流れが追えるようにする

### 簡略化しすぎない

- 「必ず」「常に」で実装差を潰さない。正しさはぼかさず、幅があるところだけぼかす
- 比喩だけで終わらせない。比喩のあとに、何を確認すればよいかを書く
- 省略語や業務略語は、初出で展開する

```
# 悪い（負荷が高い）
LB 手前で SSL 終端し、WAF で遮断されたら upstream に届かず access.log も出ないことが多い。

# よい（段階を分ける）
SSL 終端は、アプリより手前で行われることが多いです。ロードバランサ（LB）や CDN でも解く構成があります。
WAF で遮断されたリクエストは、アプリまで届かないことが多く、アプリのログには出ません。
```

```
# 悪い（正しさを落とす）
ping が通れば HTTP も必ず通る。

# よい
ping が通っても HTTP は通らないことがあります。層が違うので、届いた箱ごとに確認しましょう。
```

図・ラボ・クイズも本文と同じ基準にする。読者が図だけ見ても、断定しすぎないよう「例」「多い」を使う。

## 一般論と実装の書き分け（常時適用）

HTTP の約束やよくある型は一般論として書いてよい。アプリが実際にどう応答するかは実装次第なので、因果を決め打ちしない。

ステータスコードそのものの読み方と、業務上の失敗が何を返すかは別である。

```
# 悪い
権限が足りないと 403 になる。
未ログインなら必ずログイン画面へ飛ばされる。

# よい
権限が足りないときの画面やステータスコードは、アプリによって違う。
403 は「権限が無い」と読むステータスコードであって、権限不足が必ず 403 になるわけではない。
未ログインでは 401 やログイン画面が多いが、決まりではない。
```

書いてよい一般論の例: リクエストとレスポンスの往復、`at` 行の右端がソース位置、GET は取得が期待される。

実装次第としてぼかす例: 権限不足・認証失敗のステータスコード、エラー画面の文言、JSON か HTML か、例外をどう包むか、Spring 固有の動きを Java Web アプリ全体の話にすること。CSRF 失敗の応答（403 が多いが、302 やログイン HTML もある）。

本文が書き分けていても、図・ラボ・クイズが断定していると読者は図を信じる。典型を出すなら「例」と書く。申請くん固有ならその旨を書く。

「よくある」「多い」「アプリによって違う」は、本当に幅があるときにだけ使う。事実までぼかさない。

## シナリオは結論まで書く（常時適用）

障害調査なら原因、影響調査なら当たる箇所と直す対象を、書く前に1つ決める。本文は一般的な次の一手で止めず、その結論に至る観測と根拠まで例示する。

```
# 悪い
新しいリクエストが無いので JS を疑いましょう。（原因は書かない）
例外が出た。次はコンソールです。（特定しない）
原因のあとに、いま辿った手順と結びつかない箇条書きを置く。

# よい
新しいリクエストが無い → コンソールの TypeError → list.js が無い id を読む → これが原因。
```

- 観測（画面・Network・ログ）から、決めた結論へ一本でつなぐ
- 手法の要約が必要なら、いま特定した事実の直後に置く。先に一般論だけ置いて終わらない
- クイズも、その回で辿った観測と結論に合わせる

### 申請くん

シナリオの都合で、ソース・モック・キャプチャ用スクリプトを変えてよい。教材の抜粋や実ファイルと差が出てよい。差は `shinsei-kun/NOTES.md` に書く。

決めた原因を、サンプルの不具合として「直して」消さない。詳細は動かし、一覧だけ止める、のように範囲を限ってよい。

## 説明用の画像（常時適用）

ラベル付きの構造説明（URL 分解、スタックの 1 行、マッピング、Cookie とセッション、層、切り分け）は、CSS 図と `src/components/Icon.tsx` のアイコンにする。

申請くんの画面と Network タブだけ、写真を `public/images/screen-*.jpg` に置く。手順は下の「教材の画面キャプチャ」。雰囲気のフリー素材（ノート PC、机、南京錠、サーバ室）は置かない。

`src/` や CSS から参照するファイルは、必ず `public/images/` に実体を置く。出典表に無いファイル、参照の無いファイルは置かない。追加・削除のあと `npm run check` で参照と出典の一致を確認する。

装飾のために全ページへ貼らない。

## コードスパン（常時適用）

本文の `` `...` `` は、読者がソース・ログ・Network・HTML で同じ字面を探すときに付ける。等幅と薄い背景だけ。文字色は本文のまま。

用語ヒント（点線）とは別。概念として読む語は用語集、字面として探す語はコードスパン。両方当たることもある。

### 付ける

```
`/shinsei/requests`
`GET /shinsei/requests`
`status=APPROVED`
`RequestService.java:47`
`request.getApproverId().equals(approverId)`
`th:if`
`@RequestParam`
`context-path`
`${results}`
`t_request`
```

- パス、クエリ、ヘッダ名（`Content-Type`）
- ファイル名と行番号、設定キー、SQL の識別子
- アノテーション、テンプレート属性、フォームの `name`
- メソッド呼び出しや式。読者がその行を開く手がかりになるもの

ソースやコマンドの1行を本文に書くときは、その行の字面を途中で切らない。

```
# 悪い
const tokenEl = `document.getElementById("csrfToken")`;
curl -I `http://localhost:8080/shinsei/login`

# よい
`const tokenEl = document.getElementById("csrfToken");`
`curl -I http://localhost:8080/shinsei/login`
```

HTTP メソッドやステータスコードでも、パスやヘッダと一体の字面なら付ける（`GET /shinsei/requests`、`POST .../approve`）。

### 付けない

```
# 悪い（概念なのにスパン）
`HTTP` の約束、`GET` で取る、ステータスコードは `200`

# よい（用語のまま）
HTTP の約束、GET で取る、ステータスコードは 200
```

- HTTP、GET、POST、200、Controller、セッションなど、用語として読ませる語
- 画面の日本語、見出し、タブ名（Network タブ）
- `type: "code"` の抜粋そのもの。コードブロックの `title`、画像の `alt`
- 変数を日本語の主語にしただけ（「request は DB から取る」）。式や呼び出しなら付ける
- 抜粋全体が主題のとき。特定の字面を指していないなら付けない

## カラムとレコード（常時適用）

DB の項目は**カラム**、1 件のデータは**レコード**と書く。用語集（`terms.ts`）の見出しに合わせる。

```
# 悪い（DB なのに列・行）
部署の列が無い
その条件の行が 0 件
Mapper の列名
行ロック

# よい
部署のカラムが無い
その条件のレコードが 0 件
Mapper のカラム名
レコードロック
```

**列・行のままにする**のは、DB ではないとき。

- ソース・ログ・スタックの 1 行（`RequestService.java:47`、at 行、見る行）
- Network タブの行
- HTML の表の行（各行に承認ボタン）
- 待ち行列、タブ列、列挙、文字列

検索用に用語集のエイリアスへ `列名` や `行ロック` を残してよい。本文・クイズ・図・キャプションはカラム／レコードにする。

## 教材の画面キャプチャ（常時適用・shinsei-kun のキャプチャ作業時）

ラベル付きの構造説明は CSS 図のまま。雰囲気の写真は「説明用の画像」ルール。申請くんの画面と Network タブだけ、このルールで撮る。装飾で全ページへ貼らない。

置き場は `public/images/screen-*.jpg`。本文は `kind: "screen"`。出典は `SOURCES.md`。追加・削除のあと `npm run check`。

キャプションは画面の事実に合わせる。実装次第の応答は「例」と書く。権限不足が必ず 403、とは書かない。

### 画面（ページ本体）

ヘッドレスでよい。`page.screenshot()` はページ領域だけなので、アドレスバーはスクリプトが合成する。左右の余白は切る。幅はアプリに合わせる（一覧・詳細 960px、ログイン 440px）。高さはコンテンツにクリップする。

```
# 悪い
アドレスバーが無い。ビューポートが広く、灰色の余白が左右に残る。

# よい
URL が見える。ウィンドウ幅はアプリの幅。
```

再撮影（申請くん起動中。モックだけなら `--mocks-only`）:

```bash
node shinsei-kun/scripts/capture-screens.mjs
```

パラメータ章用の切り出しは、同じスクリプトが `crop-jpeg.ps1` で作る。`screen-history-search-query.jpg` はアドレスバーと検索フォーム（結果表は切る）。`screen-detail-path.jpg` はアドレスバーと申請IDまで。

実アプリから撮る: login / login-error / list / 申請履歴（ステータス承認済み） / detail（ID 12）/ 承認 500（ID 16）/ 承認済みの業務メッセージ（ID 11）/ not-found（99999）。
起動中のアプリでは出しにくい見え方（0 件、CSS 無し、権限エラー画面）は `shinsei-kun/src/main/resources/static/demo/` を `file://` で撮る。実行中コンテナは再ビルドしないと `/demo/**` を配信しない。画面にエラーが出ているがリクエストが無い見え方は、ページ単体ではなく Network のウィンドウ全体で撮る。

ページ画像のアドレスバーは合成（macOS 風の三点）。Network タブは Windows の実 Chrome。枠の見た目は揃えない。

承認の `window.confirm` で puppeteer の遷移待ちが落ちることがある。権限エラー画面はモックで撮る。申請履歴は山田で件名「申請」、ステータス「承認済み」を検索して撮る。PENDING のレコードも出るのが意図どおり。`node shinsei-kun/scripts/capture-screens.mjs --history-only`（検証用ホストなら `--verify`）。

### Network タブ

`page.screenshot()` に DevTools は入らない。偽の DevTools HTML は置かない。利用者は headed Chrome で PC を操作してよい、と言っている。

```bash
node shinsei-kun/scripts/capture-network.mjs
```

要点:

- Chrome は `C:\Program Files\Google\Chrome\Application\chrome.exe`。`puppeteer-core` は `shinsei-kun/` の Node 依存（Java アプリの依存ではない）
- `headless: false`、`--auto-open-devtools-for-tabs`。`devtools: true` は別ウィンドウになりやすいので使わない
- 専用プロファイル。Network を右ドック。パスワード漏洩警告は Preferences と `--disable-features=PasswordLeakDetection` で止める
- ウィンドウ全体は `window-shot.ps1`（PID でこの Chrome だけ撮る）。見える枠は `DwmGetWindowAttribute` で取る（`GetWindowRect` だと左右に後ろのウィンドウが写る）
- CSS 404 は `app.css` を intercept して 404 にし、リロードする。ページが崩れた状態と Network の 404 を同時に撮る
- `screen-network-login-fail.jpg` は `http://intranet.example.co.jp:8080/shinsei/login` を撮る。検証用シナリオ向けの画面・Network も同じホスト名。撮影 PC の hosts で `127.0.0.1 intranet.example.co.jp` を足す（`shinsei-kun/scripts/setup-capture-hosts.ps1`、管理者 PowerShell）。一括再撮影は `capture-screens.mjs --verify-scenarios` と `capture-network.mjs --verify-scenarios`
- HTML / CSS / JS が並ぶ Network は `--rows-only`（検証用ホストなら `--verify`）。撮影前に Network パネルを選ぶ。Elements タブのまま撮らない
- 申請履歴は `--history-search-only`（検証用ホストなら `--verify`）。クエリの `title=申請` と `status=APPROVED`、PENDING のレコードが同時に見える状態で撮る。検索 GET のクエリ詳細は手動。スクリプトがダイアログで案内する。`screen-network-history-search-request.jpg` は Payload の Query String Parameters から `crop-jpeg.ps1` で切り出す
- 承認 500 は、山田で ID 16「研修参加」を開き、実際に承認を送信して撮る。`request.getApproverId()` が null のため、実アプリが 500 を返す
- POST 後にログが消えるので、403 / 500 は遷移後の document 行を撮る（preserve log は必須ではない）
- 画面にエラーが出ているが POST が無い見え方は、ページ単体ではなく Network のウィンドウ全体で撮る。`screen-network-js-error.jpg` は Network のログを消してから押す。ログ消去は手動。スクリプトがダイアログで案内する（`capture-network.mjs` の js-error 撮影）。キャプションは「例」

### 起動とアカウント

```bash
cd shinsei-kun && docker compose up --build
```

http://localhost:8080/shinsei/login 。ホスト JDK は 8 でも、アプリはコンテナの Java 17。パスワードはすべて `password`。山田（`yamada`）は申請者。承認ボタンは見えるが、押すと `ForbiddenException`（403）。ID 12 を承認したら再起動するか `PENDING` に戻す。

---

## 以下は対象ファイルを編集するときだけ適用

### 教材の参照と構成
**対象**: `src/content/**/*.ts`, `src/data/**/*.ts`, `src/lib/search.ts`, `README.md`, `scripts/check-content.mjs`

章の追加・改名・削除をしたら、次を同じ変更に含める。

- `src/data/curriculum.ts` の `tracks`
- サイドバーが読む章データ（curriculum 経由）
- `src/lib/search.ts` の索引（トップ・章ページ・項目）
- `README.md` の章一覧

図は本文と矛盾させない。手前の HTTP サーバは「任意」であることが本文にあれば、シナリオの層図でも必須に見せない。

未使用の `Track.accent` を足さない。使うならカードや章見出しで参照する。

教材・クイズ・画像パスを変えたら `npm run check` を実行する。クイズ ID、図の名前、`public/images/` の実体と `SOURCES.md` を見る。

### 教材サンプルの形
**対象**: `src/content/**/*.ts`, `src/data/**/*.ts`, `src/components/Diagram.tsx`

疑似コードと明示しない限り、Java はコンパイルできる形で書く。アノテーションだけをクラスに並べない。

```
# 悪い
@Controller
@RequestMapping("/requests")
public class RequestController {
  @GetMapping
  @GetMapping("/{id}")
  @PostMapping("/{id}/approve")
}

# よい
@Controller
@RequestMapping("/requests")
public class RequestController {
  @GetMapping
  public String list(...) { ... }        // GET /requests

  @GetMapping("/{id}")
  public String detail(...) { ... }      // GET /requests/12
}
```

スタックは例外が最上段、下へ行くほど呼び出し元。Service の AOP プロキシは、その Service の直下に置く。

Spring Security を使う POST フォームには CSRF 用 hidden を書く。`th:action` なら自動挿入が多い、と補足してよい。

クイズは、その項目が教えたことの確認にする。処理の入口の特定の項で「遅さ」を問わない。

### 教材の日本語
**対象**: `src/content/**/*.ts`, `src/data/**/*.ts`, `src/pages/**/*.tsx`, `src/components/**/*.tsx`, `README.md`

HTTP の GET / POST と、Java の処理のまとまりは、どちらも「メソッド」と呼ばれる。紛らわしい箇所では必ず区別する。

```
# 悪い
どの URL・HTTPメソッドを、どのメソッドが処理するか
メソッド名 list に当たっても

# よい
どの URL と HTTP メソッド（GET など）を、どの Java メソッドが処理するか
Java のメソッド名が list でも
```

列挙のあと、助詞の前で読点を切らない。

```
# 悪い
届いていない、を疑います
誰か、を確認する
おかしい、以外の

# よい
届いていないことを疑います
誰であるかを確認する
おかしい以外の
```

- 接続は `つなぐ` / `つながる`。`繋` は使わない
- 和文と英数字の間は空ける（`HTTP メソッド`、`0 件`）
- `サーバ` / `ヘッダ` / `フィルタ`。否定は `無い`
- 画面の操作者は `利用者`。`ユーザ` は「ログインユーザ」などとセットで使う
- HTTP の 200 / 404 / 500 は `ステータスコード`（正式名称は `HTTPステータスコード`、空白なし）。どちらも同じ用語ヒントにする。`番号` や `レスポンスコード` にしない（行番号・ポート番号と紛らわしい）

同じ短い段落で、画面に出ることと調査を、どちらも「見る」で重ねない。見るは使ってよい。表示を全部「出る」にしない。

```
# 悪い
画面に見えるのは HTML です。不具合のときはリクエストを見ます。

# よい
画面に出ているのは、返ってきた HTML です。不具合のときは、先にリクエストとレスポンスを確認しましょう。
画面がおかしく見えても、原因はフロントとは限りません。
ボタンが見えないことと、処理が無いことは別です。
```

- `見た目`（色やレイアウト）はそのまま使ってよい

読者への調査手順は、説明の「します」ではなく行動を促す「しましょう」にする。仕組みの説明は「します」のまま。

```
# 悪い
不具合のときは、このリクエストとレスポンスを確認します。

# よい
不具合のときは、このリクエストとレスポンスを確認しましょう。
ブラウザはサーバへリクエストを送ります。
```

コミット時に、修正した文章に主述のねじれやてにをはの誤りがないか再確認する。

### 申請くんは教材用サンプル
**対象**: `shinsei-kun/**`

`shinsei-kun/` は、教材の画面キャプチャとソース追跡のためのサンプルアプリである。本番アプリではない。レビューや改修では、教材に出る動きが追えることを優先する。

#### 求めすぎない

テスト網羅、層の厳密な分離、国際化、監視、メール再送、パフォーマンス、セキュリティ診断の完遂などは、指摘も実装もしない。足りない設定やクラスは、動かすため・教材の抜粋に合わせるために足してよい。

```
# 悪い（過剰）
承認の監査ログ基盤を足す。DTO と Entity を全画面で分ける。カバレッジ 100% を求める。

# よい
ログイン、一覧、詳細、承認、JSON API が教材の URL で動けば足りる。
```

#### 露骨に悪くしない

動かすための簡略はよい。読者が真似すると危険な書き方や、動かない・読めないコードは避ける。

```
# 悪い（露骨）
パスワードを平文で置く。CSRF を外したまま POST する。SQL を文字列連結する。コンパイルできない。

# よい
教材の抜粋に沿う。抜粋に無い穴（ログインユーザ、初期データなど）は、普通に動く書き方で埋める。
```
