<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.example.shinsei.mapper.UserMapper">

  <select id="findByUsername" resultType="UserEntity">
    SELECT id, username, password, display_name, email, role
    FROM t_user
    WHERE username = #{username}
  </select>

  <select id="findById" resultType="UserEntity">
    SELECT id, username, password, display_name, email, role
    FROM t_user
    WHERE id = #{id}
  </select>

  <select id="findAll" resultType="UserEntity">
    SELECT id, username, password, display_name, email, role
    FROM t_user
    ORDER BY id
  </select>
</mapper>
